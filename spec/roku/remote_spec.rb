require 'spec_helper'

describe Roku::Remote do
  it 'has a version number' do
    expect(Roku::Remote::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
